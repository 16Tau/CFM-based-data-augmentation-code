Hello,

The current folder contains the source code files corresponding to Figure 8 in the paper.

**Files in this folder:**
- `generate_densenet_gradcam.py` - GradCAM visualization generation for DenseNet

**Usage:**

Step 1: You need to modify the corresponding parameters in `generate_densenet_gradcam.py`, including paths to image folders, model weights, and other configuration settings. For detailed setup instructions, refer to the comments in the source code.

Step 2: Run `generate_densenet_gradcam.py` to generate GradCAM visualizations for understanding model attention regions.

Step 3: The generated visualizations can be used to analyze the model's decision-making process.
